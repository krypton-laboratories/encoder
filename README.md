# Kryptographer

Build instructions:
- Ensure that `clang` is installed
- Ensure that `build-essential` or your platform equivalent is installed

## Building
```
make dev
./app
make clean
```
This will build with development parameters, run, and then clean the dev environment
